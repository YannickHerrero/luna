# Copy this file to ~/.config/luna/server.env and set mode 600.
# Never commit the populated file.

LUNA_PUBLIC_ORIGIN=https://your-mac.example.ts.net:8447
LUNA_ALLOWED_TAILNET_LOGINS=you@example.com

# Pi model used once to generate a contextual title for each new conversation.
# LUNA_TITLE_MODEL=openai-codex/gpt-5.6-luna

# Account-level weekly usage comes from the locally authenticated Codex app-server.
# No Codex token is copied into Luna configuration or returned by the API.
# LUNA_CODEX_EXECUTABLE=codex
# LUNA_OPENAI_USAGE_CACHE_SECONDS=300

# Device-targeted Apple Push Notification service. Leave all three metadata values and
# the key path unset to disable delivery. Store the private .p8 key outside the repository
# with mode 600; never paste its contents into this file.
# LUNA_APNS_KEY_PATH=~/.config/luna/credentials/AuthKey_KEYID.p8
# LUNA_APNS_KEY_ID=KEYID
# LUNA_APNS_TEAM_ID=TEAMID
# LUNA_APNS_TOPIC=com.yannickherrero.luna

# OpenAI-compatible speech-to-text. Leave the key empty to disable transcription.
LUNA_TRANSCRIPTION_API_KEY=
LUNA_TRANSCRIPTION_MODEL=gpt-transcribe
# LUNA_TRANSCRIPTION_BASE_URL=https://api.openai.com/v1

# Optional overrides:
# LUNA_DATA_DIR=~/Library/Application Support/Luna Server
# LUNA_EVENT_RETENTION_DAYS=30
# LUNA_ATTACHMENT_RETENTION_DAYS=30
