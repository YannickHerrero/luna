{
  "name": "@luna/protocol",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "generate": "cargo run --manifest-path ../../Cargo.toml -p luna-protocol --bin export_openapi && openapi-typescript generated/openapi.json -o src/generated.ts && prettier --write generated/openapi.json src/generated.ts",
    "test": "vitest run tests",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "typebox": "^1.1.5"
  },
  "devDependencies": {
    "openapi-typescript": "^7.13.0"
  }
}
