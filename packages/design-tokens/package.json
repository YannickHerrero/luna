{
  "name": "@luna/design-tokens",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "generate": "tsx scripts/generate.ts",
    "test": "vitest run tests --exclude dist/**",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "tsx": "^4.21.0"
  }
}
